{
    "name": "bs-moment-duration-format",
    "version": "1.0.0",
    "main": "lib/js/src/MomentDurationFormat.js",
    "license": "MIT",
    "dependencies": {
        "moment-duration-format": "^2.2.2"
    },
    "devDependencies": {
        "@glennsl/bs-jest": "^0.4.5",
        "bs-moment": "^0.4.0",
        "bs-platform": "^4.0.8",
        "moment": "^2.22.2"
    },
    "scripts": {
        "build": "bsb -make-world",
        "start": "bsb -make-world -w",
        "clean": "bsb -clean-world",
        "pretest": "yarn build",
        "test": "jest",
        "lint": "prettier '{src/**,.}/*.{yml,re,json,md}' --write",
        "prepublish": "yarn clean && yarn build"
    }
}
